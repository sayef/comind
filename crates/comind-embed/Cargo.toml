[package]
name = "comind-embed"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Local static-embedding (Model2Vec) of code symbols — pure Rust, CPU, no ONNX."

[dependencies]
anyhow.workspace = true
comind-core = { workspace = true }
# Model2Vec static embeddings: pure-Rust, CPU, tiny + fast (semble's approach), no ONNX runtime.
model2vec-rs = "0.2.1"
